{
  "actorSpecification": 1,
  "name": "seo-audit-etl",
  "title": "SEO Audit ETL (ZIP → normalized JSON + scores)",
  "description": "Downloads a dated audit ZIP, parses CSV/JSON with robust fallbacks (Ahrefs UTF-16 TSV, nested zips), aggregates small metrics, computes proportional OSS/LSS with coverage, and outputs normalized_audit.json, scores.json, etl_manifest.json.",
  "version": "0.1",
  "buildTag": "latest",
  "nodejsVersion": "20.0.0",
  "input": {
    "title": "Audit ETL Input",
    "description": "Provide the client details and a public/signed URL to the dated audit ZIP.",
    "schemaVersion": 1,
    "type": "object",
    "properties": {
      "client": {
        "title": "Client slug",
        "description": "Short identifier for the client (e.g., acme).",
        "type": "string",
        "editor": "textfield"
      },
      "domain": {
        "title": "Primary domain",
        "description": "The site's primary domain (e.g., acme.com).",
        "type": "string",
        "editor": "textfield"
      },
      "runDate": {
        "title": "Run date",
        "description": "Date label for this audit run (YYYY-MM-DD).",
        "type": "string",
        "editor": "textfield"
      },
      "zipUrl": {
        "title": "ZIP URL",
        "description": "Public or signed URL to the dated audit ZIP (e.g., Google Drive direct download link).",
        "type": "string",
        "editor": "textfield"
      }
    },
    "required": ["client", "domain", "runDate", "zipUrl"]
  }
}
