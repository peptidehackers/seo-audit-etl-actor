{
  "actorSpecification": 1,
  "name": "seo-audit-etl",
  "title": "SEO Audit ETL (ZIP → normalized JSON + scores)",
  "version": "0.1",
  "buildTag": "latest",
  "nodejsVersion": "20.0.0",
  "input": {
    "title": "Audit ETL Input",
    "description": "Provide the client info and a public/signed URL to the dated audit ZIP.",
    "schemaVersion": 1,
    "type": "object",
    "properties": {
      "client": {
        "title": "Client slug",
        "description": "Short identifier for the client (e.g., acme).",
        "type": "string"
      },
      "domain": {
        "title": "Primary domain",
        "description": "The site domain for this audit (e.g., acme.com).",
        "type": "string"
      },
      "runDate": {
        "title": "Run date (YYYY-MM-DD)",
        "description": "The date label for this audit run, used in outputs.",
        "type": "string"
      },
      "zipUrl": {
        "title": "ZIP URL",
        "description": "Public or signed URL to the dated audit ZIP (Google Drive direct download or similar).",
        "type": "string"
      }
    },
    "required": ["client", "domain", "runDate", "zipUrl"]
  }
}
